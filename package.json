{
  "name": "australia-post",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "postcss:watch": "postcss src/styles/tailwind.pcss -o src/styles/tailwind.scss --env development -w",
    "postcss:build": "postcss src/styles/tailwind.pcss -o src/styles/tailwind.scss --env production",
    "parcel:serve": "parcel src/index.html",
    "parcel:watch": "parcel watch src/index.html",
    "parcel:build": "parcel build src/index.html",
    "clean": "rm -rf dist .cache",
    "build": "npm-run-all -s clean postcss:build parcel:build",
    "dev": "npm-run-all -s clean -p postcss:watch parcel:serve"
  },
  "devDependencies": {
    "@babel/core": "^7.8.4",
    "@babel/preset-env": "^7.8.4",
    "npm-run-all": "^4.1.5",
    "parcel-bundler": "^1.12.4",
    "postcss-cli": "^7.1.0",
    "postcss-fontpath": "^1.0.0",
    "posthtml-expressions": "^1.1.1",
    "posthtml-include": "^1.3.3",
    "sass": "^1.25.0",
    "tailwindcss": "^1.2.0"
  }
}